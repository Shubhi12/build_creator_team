Generic single-database configuration for Alembic database migrations.

To apply migrations:
  alembic upgrade head

To create a new migration:
  alembic revision --autogenerate -m "description of changes"
